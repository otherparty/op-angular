import { TermsAndConditionsDirective } from './terms-and-conditions.directive';

describe('TermsAndConditionsDirective', () => {
  it('should create an instance', () => {
    const directive = new TermsAndConditionsDirective();
    expect(directive).toBeTruthy();
  });
});
